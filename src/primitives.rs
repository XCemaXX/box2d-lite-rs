#[derive(Debug)]
pub struct Rectangle { // pos in the left top corner
    pub x: f32,
    pub y: f32,
    pub w: f32,
    pub h: f32,
}

#[derive(Debug)]
pub struct Point {
    pub x: f32,
    pub y: f32,
}